schema: './graphql/schema.graphql'
documents: './graphql/queries/*.graphql'
generates:
  ./api/types.ts:
    plugins:
      - typescript
  ./api/hooks.ts:
    plugins:
      - add:
          content: "import { Exact, Scalars, CreateTripInput, UpdateTripInput, CreateTourInput, UpdateTourInput, CreatePlaceInput, UpdatePlaceInput, CreatePostInput } from './types';\n"
      - typescript-operations
      - typescript-react-apollo
    config:
      withHooks: true
      withComponent: false
      withHOC: false
  ./graphql/generated/schema.d.ts:
    plugins:
      - typescript-graphql-files-modules
    config:
      modulePathPrefix: '@graphql/'
