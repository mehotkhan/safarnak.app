mutation Login($username: String!, $signature: String!, $deviceId: String!, $publicKey: String) {
  loginUser(username: $username, signature: $signature, deviceId: $deviceId, publicKey: $publicKey) {
    user {
      id
      name
      username
      status
      createdAt
    }
    token
  }
}
