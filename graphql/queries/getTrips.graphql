query GetTrips($status: String, $isHosted: Boolean) {
  getTrips(status: $status, isHosted: $isHosted) {
    id
    userId
    destination
    startDate
    endDate
    status
    travelers
    preferences
    budget
    accommodation
    isHosted
    createdAt
    updatedAt
  }
}


