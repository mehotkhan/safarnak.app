query GetTrips($status: String, $isHosted: Boolean) {
  getTrips(status: $status, isHosted: $isHosted) {
    id
    destination
    startDate
    endDate
    status
    travelers
    preferences
    budget
    accommodation
    createdAt
    updatedAt
  }
}


