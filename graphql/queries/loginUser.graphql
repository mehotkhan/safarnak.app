mutation LoginUser($username: String!, $signature: String!) {
  loginUser(username: $username, signature: $signature) {
    user {
      id
      name
      username
      createdAt
    }
    token
  }
}


