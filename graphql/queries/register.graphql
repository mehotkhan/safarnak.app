mutation Register($username: String!, $publicKey: String!, $signature: String!, $deviceId: String!) {
  registerUser(username: $username, publicKey: $publicKey, signature: $signature, deviceId: $deviceId) {
    user {
      id
      name
      username
      status
      createdAt
    }
    token
  }
}
